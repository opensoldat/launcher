import { SoundSettings } from "../types";

const defaultSoundSettings: SoundSettings = {
    // [0, 100] range.
    volume: 50,
    battleSoundEffects: false,
    explosionsSoundEffects: false
}

export default defaultSoundSettings;