class ShowSettingsHandler {
  handleMessage() {
    // TODO: bring back main window to focus, and send message to renderer,
    // so it can update its state to show settings tab
  }
}

export default ShowSettingsHandler;
